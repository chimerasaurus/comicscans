opencv-python>=4.8
Pillow>=10.0
numpy>=1.24
fastapi>=0.110
uvicorn>=0.29
scipy>=1.11                # optional — for comiceval.py parameter tuning

# External (not pip): tesseract — required for --auto-rotate
# Install via: brew install tesseract
